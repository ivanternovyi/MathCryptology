<p>
  <%= link_to 'Головна', root_path %>
</p>
<div class="text-center">
  <h1>RSA з використанням бінарного алгоритму піднесення до степеня за модулем:</h1>
  <% if @show %>
    <p>Введений текст: <br/> <%= Result.last.before %></p>
    <p>Текст після опрацювання: <br/> <%= Result.last.after %></p>
  <% end %>

  <%= form_tag(compute_rsa_index_path, method: :post) do -%>
    <p class="text-bold mt">Введіть текст</p>
    <div class="mt">
      <%= text_area_tag 'body', nil, size: '80x15' %>
    </div>
    <div class="mt"><%= submit_tag 'Опрацювати' %></div>
  <% end -%>
</div>
